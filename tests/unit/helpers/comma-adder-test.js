import { commaAdder } from 'mosk/helpers/comma-adder';
import { module, test } from 'qunit';

module('Unit | Helper | comma adder');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commaAdder([42]);
  assert.ok(result);
});
